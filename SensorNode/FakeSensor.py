import math


def collect_data(index_value):
    """
    :param index_value: value to assign the key of the dictionary entry
    :return: value: In this case, value is simply calculated from sin to give some generic data
    """

    # Simply generate data
    value = (math.sin(index_value))

    return value
